{
  "name": "client",
  "version": "0.1.0",
  "homepage": "https://AmberMichaelis.github.io/react-gh-pages",
  "private": true,
  "dependencies": {
    "mysql2": "^2.3.3"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
