{
  "name": "client",
  "version": "0.1.0",
  "homepage": "https://ambermichaelis.github.io/reactjs_mysql_nodejs_employee_database/",
  "private": true,
  "dependencies": {
    "mysql2": "^2.3.3"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
